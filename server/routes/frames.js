/*const express = require('express')
const {
  getFrames,
  generateFrame,
  searchFrames,
  getFramesPagination
} = require('../controllers/frameController')

const router = express.Router()
// GET all frames
router.get('/', getFrames)
// GET all frames
router.post('/add', generateFrame)
//search frames
router.get('/search',searchFrames)
//get frames by pagination
router.get('/pagination',getFramesPagination)

module.exports = router*/